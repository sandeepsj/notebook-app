# Google OAuth Web Client ID (from Google Cloud Console).
# Copy this file to `.env` and fill in your value for local dev.
# In CI, set the same value as the GitHub Actions secret VITE_GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
